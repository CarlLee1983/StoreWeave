# Spec 0008 實作交接

此文件供切換模型或開啟新對話後接手；規格與驗收以各 Issue／Spec 正文為準，這裡只記入口與交接狀態。

## 目前狀態（2026-09-07）

- 專案：`CarlLee1983/StoreWeave`；本機工作目錄 `/Users/carl/Dev/Carl/StoreWeave`。
- 已完成架構審視、Spec 0008、Ticket 81–90 的本機實作、驗收與獨立複核，未 commit／push。Ticket 90 Sol/high 整票 PASS；Base B00–B01 亦已完成，下一階段 B02，見 [Base 計畫](base-implementation-plan.md)。
- [總單 #17](https://github.com/CarlLee1983/StoreWeave/issues/17) 收錄完整規格、順序與所有子工單連結。
- 本輪實作是 GitHub #18–#27（本地 Ticket 81–90）。GitHub #28／Ticket 91 是 ForgeFlowv2 調研，缺來源與場景，不阻擋本輪。
- 規格、工單與索引已存在本機，尚未 commit／push；保留這些未提交文件，不視為無關產物刪除。
- Ticket 81 實測：`pnpm typecheck`、`pnpm typecheck:admin`、`pnpm test:admin`（19 檔、184 tests）與 `pnpm build:admin` 通過。其後 Ticket 82 全庫 `pnpm test` 已通過（48 檔、561 tests）；不再把先前 CLI JSON 失敗列為現況。
- 本輪實作已授權持續執行 Ticket 82–90，後續按 Base DAG 執行 B00–B17；不需要每票重新確認。所有新 Herdr Codex implementation／review pane 以 `--dangerously-bypass-approvals-and-sandbox` 啟動，仍禁止 commit、push、merge、GitHub 寫入、部署與商家資料刪除。

### 2026-09-07 Ticket 86 完成驗收

- 本機完成 Ticket 86：token 草稿生命週期、palette 鍵盤／ARIA／捲動、ERP empty state／翻譯／columnheader、七頁三語與局部表格捲動、窄版 topbar／Login 已整合。auth／token storage／roles／原 API 與 retry 允許條件未改。
- 最終 `pnpm typecheck:admin`、`pnpm test:admin`（23 files／210 tests）、`pnpm build:admin`、`git diff --check` 通過。root `pnpm typecheck`／`pnpm test`（48 files／561 tests）亦通過。
- 獨立 Terra/high source review 及 ERP／Login 後續增量複核均 PASS。主代理核對 `/tmp/storeweave86-reviewed-source-final.json`，終審後 source 無變動。
- 本機 Chromium／隔離 fixture：16 routes，palette／token／ERP drawer 鍵盤與回焦、empty/loading/error、三語 × 雙主題 × 360/1280（Login、shell 與七頁），19 組結果全通過、39 張截圖；主代理目視代表圖並獨立確認 Contact 操作按鈕捲入後完全可見。Login 卡片、欄位、按鈕、footer 子元素另有 viewport bounding gate，已修日文窄版裁切。
- 證據與重跑腳本：`/private/tmp/storeweave-ticket86-playwright.lv9BsY/{browser-smoke.mjs,mock-api.mjs,results.json,screenshots}`；最後 browser log `/tmp/storeweave86-browser-run.log`。mock／Vite 已停止；實作與審查 panes 完成後關閉。重跑方式見 Ticket 86。
- **仍待 Ticket 90 closure：** `pnpm test:integration` 58 files 中 57 passed／1 failed，505 tests 中 504 passed／1 failed。`tests/integration/promotion-crud.test.ts:183` 預期折扣 1,000、實際 2,800；從 `1f4470d` 匯出的未修改快照執行同檔案亦重現（9 passed／1 failed）。共用 harness 的前例建立 2026-09 八折活動，後例再疊九折；需修測試隔離，不改促銷 runtime。log：`/tmp/storeweave-integration-20260907.log`。
- **仍待 Ticket 90 closure：** 隔離 `pnpm smoke:docker` 61 passed／1 failed；`scripts/smoke.sh:190` 預期 7 個事件，metadata 實際回傳 19。腳本、bundle／meta controller 相對 HEAD 未改；需核對事件契約後更新 smoke 斷言。獨立 Compose project `storeweave-smoke86-20260907`／image tag `smoke86-20260907`／port 33186，測試 containers／volumes 已清理。log：`/tmp/storeweave-smoke86-docker.log`。native smoke 本票未跑；不宣稱全庫 CI 通過。

### 2026-09-07 Ticket 87 完成驗收

- Terra/high 實作與 Sol/high 獨立審查完成；首輪 Medium 商品 read-error／retry 語意問題已修正，增量終審 Code／Standards／Spec PASS，無剩餘發現。分析 `/tmp/storeweave-ticket87-sol-high-analysis.md` 與實作前快照見 Ticket 87。
- Query／商品四種 mutation 已接合；memory-only 未確認操作跨 route／locale 保留原 payload/key，身分切換使舊 handle 失效並清 cache。已修原草稿恢復、反覆 retry／拒絕解鎖、server-applied response-lost replay、recovered dialog 焦點與 inventory-error 新操作入口。
- 最終驗證通過：Admin 25 files／233 tests、root 48 files／561 tests、兩者 typecheck、Admin build、diff check。主代理 Chromium 完整 12/12 matrix 通過，執行前後 41 個 runtime 檔案 hash 一致：`/tmp/storeweave87-primary-browser.LveVUC/{check.mjs,results-all.json,full-run.log,source-before.json,source-after.json}` 與 screenshots。基本四種 command smoke 在 `/private/tmp/storeweave-ticket87-playwright.kHFdyy`。
- 主代理確認 87 全部 AC，保存 68 個 source/test/manifest hash 於 `/tmp/storeweave87-reviewed-source-final.json`。實作／審查 panes 與 localhost:5175 測試 Vite 已清理；沒有 commit、push 或 GitHub 寫入。下一張 88 依 Spec 0008 做 Sol/high 高風險分析、Terra/high 實作與 Sol/high 獨立審查。

### 2026-09-07 Ticket 88 完成驗收

- 最終 Sol/high 整票 Standards／Spec PASS，零發現；主代理 Admin286／root561、typechecks／build／diff PASS，20 commands＋13 marketing＋9 read-layout 全批次與新增 held-refetch／repeated-unknown browser PASS。76 final hashes `/tmp/storeweave88-reviewed-source-final.json` 穩定，完整證據見 Ticket88 Final acceptance。以下保留迭代歷程，較早 in-progress／待驗狀態已由本段取代。

- 已核對 authenticated `gh issue view 25` 與本地規格一致；沒有 GitHub 寫入。修改前 baseline：`/var/folders/mp/2hbmdcp15qjfn3fhgctttgl40000gn/T/storeweave-ticket88-baseline-lug7_lia`，禁止在其中安裝或共享可變 node_modules。
- Sol/high 分析已完成：`/tmp/storeweave-ticket88-sol-analysis.md`。主代理確認八頁 keys／conditional invalidation 與單一 memory-only operation lifecycle；分析 pane 已關閉。
- Foundation穩定後拆成兩個互斥頁面切片：Herdr `admin88`／`wA:p11` 擁有 Orders／Customers／RMA／Shipping＋全部 shared API/query/i18n/ReasonDialog/foundation；`marketing88`／`wA:p13` 僅擁有 Promotions／Coupons／Loyalty／BrandContent與各頁tests。兩者 Terra/high YOLO no-alt-screen，各檔單一writer；任務 `/tmp/storeweave-ticket88-implementation-assignment.md`（頂部ownership更新）與 `/tmp/storeweave-ticket88-marketing-assignment.md`。目前持續八頁 Query／命令遷移，正建立 Orders／RMA 共用命令；命令／整票測試尚未完成；保留其工作，不因觀測 timeout 重啟。
- Foundation 初測 Admin 25 files／234 tests PASS；主代理既有87 Chromium12/12 PASS，證據 `/tmp/storeweave88-primary-browser/foundation87`。這些沒有覆蓋所有跨 domain 情境，不代表 foundation 終審通過。
- Sol/high `review88`／`wA:p12` 首輪 foundation-only 審查 2High／1Medium／1Low；跨 domain replay、Product重複executor、async callback identity與命名皆已修正。後續 Stock draft 重建問題也已修正；focused71tests／typecheck PASS，Sol已確認 production 問題 resolved。
- 最後一項 Low 是 repo test causality：原測試重用既有pending fetch，不能證明confirmation啟動refetch。主代理暫時接手 `admin-operations.test.ts`，改為 settled cache＋active QueryObserver，assert command前idle/零呼叫、confirmation後新refetch、identity取消訊號、清cache、新identity資料不受late結果污染；13tests／typecheck:admin／diffcheck PASS。檔案ownership已交回Terra（須保留主代理修改），Sol已獨立核對此case、13/13 PASS且hash穩定；**foundation phase PASS，所有基礎發現已解決**。7個核心source/test hash已存 `/tmp/storeweave88-foundation-reviewed-source.json`。此審查pane完成後關閉；八頁完成後另開fresh Sol做整票審查。
- RMA已建立concrete mutation hook共用於Orders／Rma，focused頁測試通過；Shipping API已接stable key參數，focused15tests通過。主代理先前抓到空篩選藏掉recovery，已修正；目前 rma-mixed-receive-response-lost 與 rma-unknown-draft-lock **兩項PASS**，包含空篩選→Orders重試、samebody/key、無重複入庫與唯讀draft跨route/locale恢復。欄位readOnly也是有效鎖定，browser已改驗不可編輯。六項RMA驗收依 `/tmp/storeweave-ticket88-rma-acceptance.md`，主代理另已目視日文360px與Orders跨頁preview，無raw JSON或整頁overflow；日文error banner過度擠窄已交實作者調整。preview完整三語／全部guard／焦點仍待完整matrix及終審；不要將signature/read-onlyQuery/partialrecovery視為完成。

- 主代理 browser 目錄 `/tmp/storeweave88-primary-browser`：fixture.mjs／check.mjs／commands.mjs。八頁 fixture preflight、coupon-partial-failure、customer-fast-search、customer-identity-pending-read、order-partial-failure PASS。這些是 moving-worktree 局部證據，穩定後需完整重跑。新增 confirmed-invalidation-identity-clear browser PASS，證據 foundation87-identity 子目錄（已確認命令後的pending refetch取消、不覆蓋新身分、不觸發額外新身分GET）；對應repo regression已由主代理補強因果性、13tests PASS，Sol foundation PASS。commands.mjs 九組 financial scenarios：兩組RMA、ecpay-retry-response-lost、old-command-identity-clear PASS；shipment-create-stage-label-recovery後續PASS，涵蓋原鍵確認後帶回物流明細、opaque label保持文字、stage replay。reward-response-lost-new-operation與tier-points-response-lost後續重跑PASS，包含相同金額的新操作換key。full-refund-response-lost／refund-retry-response-lost原缺恢復入口及修正途中orderRecovery初始化前存取crash均已修復，後續重跑PASS（退款再次失敗後明確新重試換key）。commands.mjs另新增pay-applied-response-lost與cancel-unknown-raw-draft，皆PASS；後者確認前後空白原草稿保留、request正規化與samekey。marketing.mjs 原四組目前全部PASS：promotion編輯鎖定及原payload、coupon批次重試結果、loyalty sparse PATCH、article sparse PATCH；證據marketing-all.json／marketing-run.log。marketing88恢復Terra/high後完整自查結束，focused65/65與admin typecheck PASS（worker回報）；主代理完整五組browser重跑PASS。候選8個page/test hash已存/tmp/storeweave88-marketing-reviewed-candidate.json並確認穩定；保持idle，等待獨立審查，尚非整票終審。新增第五組loyalty-unknown-remount-draft初次FAIL、修正後PASS：原始2.50草稿、重掛載鎖定、讀取失敗時可重試及同key sparse payload；所有相關drawer/form原草稿、可讀業務preview及loading recovery gate仍由marketing88補齊。shipping-partial-failure亦PASS。locales-themes-layout初次停在en-US/dark/360/shipping新增按鈕溢出，修正後96組viewport/locale/theme/page寬度檢查PASS。但主代理目視日文360px另發現Orders／Shipping／Customers表格擠壓重疊與Orders金額卡裁切，已交admin88修可讀local horizontal scroll；其後已補上表格local scroll／末欄可達／KPI文字bounding gates，全read/layout batch7/7與command batch11/11 PASS；主代理目視新日文360px Orders金額完整、table保持可讀橫向捲動，Shipping同樣修復。證據full-read-layout.log／results-all.json、full-commands.log／commands-all.json。其餘全票checks與獨立review仍待完成。overflow.mjs／overflow.log保留初次重現。ECPay harness已修正為先查詢物流單再按既有重試按鈕，這是fixture操作修正而非app bug。已加跨頁回 Products 不得出現 foreign retry 控制項的 gate。
- 新開Herdr `final88`／`wA:p14`，Sol/high YOLO no-alt-screen，先獨立read-only審查穩定的marketing八個page/test檔；commerce/shared仍由admin88收尾，穩定後擴大同一fresh reviewer至全票。不得把marketing slice審查當全票PASS。
- Sol `final88` marketing-only首輪 **NEEDS WORK：3P1+3P2**。P1 retry結果未接local busy／terminal error導致確認後仍鎖定；既有unknown scope正常入口回blocked卻無聲；coupon promotions／loyalty tiers讀取失敗仍可做依賴寫入。P2 readerror/loading落入空table、modal unknown缺dialog內Retry與enabled焦點、coupon preview缺promotion label／targetstatus。八檔hash穩定、focused65/65與diffcheck PASS不能覆蓋上述缺口；全六項已交marketing88 Terra/high修正，候選解凍，Sol留idle等增量複核，整票review仍待commerce穩定。
- 兩個Terra切片現已明確完成並idle：admin88 focused68/68（四commerce pages＋commerce-operations/api/query），marketing88focused69/69。Primary修正後完整checks全部PASS：Admin26 files266 tests、root48 files561 tests、兩者typecheck、Admin build與diffcheck；logs `/tmp/storeweave88-{admin,root}-{typecheck,test}-final.log`／`/tmp/storeweave88-admin-build-final.log`。Primary全12command＋8read/layout＋9marketing browser PASS；source candidate `/tmp/storeweave88-current-source-candidate.json`與測試後hash核對無變化。final88已收到stable signal，完成marketing增量後擴大到完整Ticket88delta／全部AC，尚未取得整票結論。Sol目前已確認helper層customer/order/RMA/shipping刷新對照符合accepted map（paid-only／restock-only／stage／dead-job分支），正在追蹤各頁進入helper的request、read-error guard與recovery；此局部結論不代表整票PASS。
- Sol整票首輪完成，**NEEDS WORK：3P1＋8P2**，完整報告 `/tmp/storeweave88-whole-review-round1.txt`。P1：財務terminal rejection仍保留舊attempt/key、expanded refund未知時允許新refund、跨area aggregate scope阻擋無回饋／shipping錯kind replay。P2：部分modal/global recovery吞terminalerror、marketing pending時解鎖與tier/remove/delete scope遺漏、commerce modal原操作retry/focus缺口、Coupon已開drawer的late selector error guard與Customer獨立loyalty、currency/status業務preview、generic key helper及完整map/signal/key regression不足。文件in-progress是預期closure待辦，PRIMARY通過前不改done。兩原Terra/high writers已依既有ownership接回全部適用修正，candidate解凍；Sol保留審查上下文、idle等增量複核。已通過的checks/hash是修正前候選證據，不能代表修正後狀態。Primary另加六組financial-{rewards,tier-points,refund}-{reject,unknown}-then-new-key瀏覽器case；direct reward reject初次已重現欄位無法解鎖，修復後三個direct rejection皆PASS；三個unknown→global retry→terminal400初次FAIL錯誤不可見，修復後完整六組financial batch全部PASS（financial-run.log／commands-financial-.json），含相同payload新鍵、terminalerror與rawdraft解鎖。驗證要求包含相同payload明確新操作換key、原始draft保留與terminal error。read harness另加refund-read-error-blocks-new-command，已PASS；ECPay原case加不同advance操作不得重放既有ECPay retry的disabled gate，初跑在moving source因operationEntries is not defined而未到驗證點，修复後ECPay case PASS（含不同advance操作disabled）。Primary另加兩組{rmas,orders}-reason-dialog-recovery：Rmas初跑FAIL unknown轉換未聚焦enabled recovery control；Orders初跑FAIL既有dialog內無Retry，已交admin88處理already-open phase transition。Marketing另加四個{promotion,coupon,article,loyalty}-pending-retry-lock案例（共13組，四組已全PASS，主代理完整13組marketing batch全PASS且candidate3八檔hash一致；/tmp/storeweave88-marketing-reviewed-candidate3.json。marketing88focused71/71、idle，final88已重啟bounded marketing增量複核，commerce/shared仍由admin88收尾），驗held same-key retry期間欄位持續鎖定，再回terminal400時dialog內可見錯誤且解鎖。
- 最新增量：Sol marketing runtime PASS，尚缺三個持久回歸案例（已開 Coupon drawer 後 selector 失敗、同 tier save/remove 佔用、article delete 佔用），marketing88 正補測試。admin88 commerce/shared 首輪修正後 typecheck 與 61 focused tests PASS；主代理重跑兩個理由 modal，Retry 與 lock 已有，但 pending→unknown 焦點仍 FAIL，已交原 writer 修 shared transition regression。final88 同時增量審查其餘 commerce/shared。root 第二輪 typecheck 與 48 files／561 tests PASS（round2 logs）；read/layout 全批次重跑中。整票仍未驗收完成。
- 第二輪全批次：Admin 26 files／274 tests、root 48 files／561 tests、兩者 typecheck、Admin build、diff check PASS；20 command、13 marketing、9 read/layout browser 全 PASS，76 檔 candidate hash 穩定（`/tmp/storeweave88-round2-source.json`）。Sol 第二輪仍 NEEDS WORK（`/tmp/storeweave88-whole-review-round2.txt`）：退款成功後 refetch 期間按鈕再啟用、Orders repeated-unknown 焦點，以及完整 map/API/key/page regression proof 缺口。Primary 新增 refund-confirmed-refetch-blocks-new-command 已重現 FAIL，兩 reason-modal cases 加 repeated unknown。admin88 修 runtime＋commerce page/component regressions；**ownership 更新：marketing88 僅接 api.test.ts／query.test.ts／commerce-operations.test.ts 三檔**補完整契約/map測試，admin88 不改這三檔。Sol marketing runtime 與新增三頁 regression 已確認 resolved。整票待此增量驗收，不以既有綠燈代替。
- 第二輪 runtime 增量已經主代理 browser 確認：refund-confirmed-refetch-blocks-new-command PASS（先前 FAIL 斷言成功重現；fixture 後續清掉既有 failureMessage 才正確觀測新 reason），Orders／RMA reason-dialog 加 repeated unknown 後皆 PASS。三檔契約/map測試與 commerce regression 仍由兩原 Terra/high writers 補齊；尚未取得 Sol round3。
- 契約/map 補測已完成並 frozen：marketing88 只改 api.test.ts／query.test.ts／commerce-operations.test.ts，18 read signals／32 command keys、完整 semantic inputs／IDs、剩餘 map branches；3 suites／16 tests、admin typecheck、diff check PASS。final88 已接 bounded round3 runtime＋此三檔 proof 複核（第一次直接 Vitest 忽略 jsdom 是命令問題，已改正 repo Admin project 重跑）。admin88 最初 round2 completion 僅補 rewards/points，主代理發現仍缺退款／跨area／wrong-kind／read-gate durable cases，已明確交回補齊；目前原 Terra/high 持續 Orders/Shipping test 修正，不能視為完成。兩者 live 工作中，timeout 只是等待期限；保留 panes，不重啟。
- Sol bounded round3 **PASS**：`/tmp/storeweave88-round3-bounded-review.txt`。兩項 runtime 與完整 API/key/map proof 已無發現；正確 Admin invocation 26 files／281 tests PASS。整票只剩 commerce page regressions 的最後獨立複核與 current-state acceptance。Primary 額外要求退款 key sequence 明確驗 call2≠call1／call3=call2／call4≠call3；held refetch 必须從成功 empty cache＋成功命令觸發，避免用 initial loading 假代。admin88 正完成此測試，不改 runtime。
- Current final candidate `/tmp/storeweave88-round3-source.json` 76 檔 hash 穩定；Primary Admin round3 26 files／282 tests、typecheck、build、diff check PASS。root round2 561 tests／typecheck仍有效，root manifest/lock/config 與88 baseline全相同。admin88 最後 page/component 66/66已完成並 frozen；final88 正合併既有 accepted audits 做最後 page-proof 增量整票審查，尚未宣告整票 PASS。三 panes保留，Vite5175仍運作，未進89。
- Sol round3 全票 runtime/spec 已接受，仍三項 P2 proof gaps：`/tmp/storeweave88-round3-proof-gaps.txt`。退款 refetch 需用有效 requested 狀態且等待回傳 row 才斷言；Customer unknown→global terminal rejection 尚未測到；Shipping需補反向 order-area→建單與advance→ECPay guards。**最新單一 writer ownership：admin88 只改 OrdersPage.test.tsx／ShippingPage.test.tsx；marketing88 只改 CustomersPage.test.tsx**，皆 Terra/high，runtime 與契約三檔 frozen。補測完再 Sol 增量，仍不進89。
- Round4 proof fixes frozen：Orders valid requested Refund 並等待 rendered row/status；Shipping 兩個 reciprocal guards＋typed operations/recovery access；Customer rewards/tier unknown→global422→error/rawdraft/newkey。兩 writers focused30＋22、admin typecheck、diff PASS。candidate `/tmp/storeweave88-round4-source.json` 76 檔，runtime保持已接受版本；final88 正只複核三測試檔並合併整票結論，primary全Admin round4重跑中。
- 曾發現marketing88原生狀態列意外變成Luna/low，已安全checkpoint，透過/model恢復並目視確認Terra/high後繼續；不得將該短段工作視為指定模型驗收，後續Terra自查與fresh Sol全票審查仍必須完成。
- 主代理第一輪必要checks PASS：root typecheck／48 files561 tests、Admin typecheck／25 files257 tests、Admin build／diff check；logs `/tmp/storeweave88-{root,admin}-{typecheck,test}.log`與`/tmp/storeweave88-admin-build.log`。Build既有chunk-size warning保留。隨後source對照仍發現customer helper刷新過廣、shipping create/retry缺精確shipment／installed ECPay刷新，已交admin88按既定map修正與新增behavior regression；修正後需對應重驗，這輪綠燈不代表全票完成。主代理進一步確認ShipmentWorkbench仍有shipment/operation/failedOperations/label/extension安裝狀態的server useState、直接api讀取與吞錯effect；已明確交admin88完整遷入Query、signal與獨立read-error（保留明確標籤查詢），不是只接stable key就完成。Shipping method recovery另須頁級入口、exact raw draft、enabled focus與可讀業務preview，一併收尾。主代理已加第12組shipping-method-unknown-raw-draft瀏覽器case（syntax PASS，初跑在moving source頁面載入timeout、未進case；marketing兩處ternary語法修復後重跑PASS，涵蓋method全草稿／lock／focus／readerror recovery）。Marketing另新增promotion-dialog-retry-success/reject與loyalty-same-mount-retry-success/reject四組，驗終局錯誤／解鎖／下一操作newkey與dialog焦點；現共9組，新增4組已跑：loyalty-same-mount-retry-success PASS；promotion-dialog-retry-success初版因預期自動關dialog失敗，調整為確認可編輯且能發新key後PASS（保留dialog是可接受行為）。兩個retry-reject初次FAIL錯誤未顯示，修復後皆PASS；主代理最終9組完整batchPASS，含terminalerror／unlock／nextkey，證據marketing-all.json。marketing88focused69/69並確認六項Sol發現已處理，idle；候選2hash /tmp/storeweave88-marketing-reviewed-candidate2.json，final88已啟動Sol增量複核。check.mjs另加shipping-operation-read-errors，已PASS（fixture先切語言再選物流單，避免切語言重掛載清掉本地selection造成假失敗），驗失敗不顯示no-record/empty成功文字、獨立Retry及零command。fixture until已支援async predicate，並新增頁面載入失敗時body/pageErrors/calls/screenshot證據與context清理。Sol審marketing時的admin typecheck因Shipping draft/request改動中暫時失敗，不能以較早通過輸出代表current state；以收尾後重跑為準。
- 主代理 localhost:5175 Vite仍供後續browser使用（exec session97411）；尚未做88修正後最終checks／stable-source確認／Sol整票終審。無 commit、push、部署或商家資料寫入。

### 2026-09-07 Ticket 89 完成驗收

- 最終 Sol/high Standards／Spec PASS，零發現；Admin26files307tests／root48files561tests、兩者typecheck／Adminbuild／diffPASS，Chromium8read／13command／84layoutPASS。76finalhashes `/tmp/storeweave89-reviewed-source-final.json`一致，完整證據見Ticket89 Final acceptance。以下保留迭代歷程，較早待驗状态已由本段取代。

- 88 已 done、Sol 整票 PASS、Admin286與全部必要驗證 PASS；三個88 owned panes及Vite session97411已清理，未提交或遠端寫入。
- 89遠端#26已核對，baseline `/var/folders/mp/2hbmdcp15qjfn3fhgctttgl40000gn/T/storeweave-ticket89-baseline-6ayo2yue`。`analysis89`／`wA:p15` Sol/high YOLO no-alt-screen 已開始 read-only 高風險分析。之後依分析確定最小邊界，再開 Terra/high實作；89→90→Base B00–B17 依原目標持續。
- Sol89 分析已完成並由primary接受：`/tmp/storeweave-ticket89-sol-analysis.txt`。DLQ只刷新dead jobs＋health，不以job type推定async domain完成；ERP加自身lists；invoice issue/void加自身lists/detail（issue為targeted best effort）；contact自身lists。新analytics keys需補pay/cancel與promotion update的受影響對照。analysis89pane已關閉；`reads89`／`wA:p16` Terra/high YOLO no-alt-screen 已啟動，任務 `/tmp/storeweave-ticket89-implementation-assignment.md`，先接shared interface，再開互斥command-page writer。
- 89 shared interface已穩定：所有scoped讀取signal、五commands必填caller key、explicit analytics/health/contact keys。`commands89`／`wA:p17` Terra/high YOLO no-alt-screen 已啟動，唯一writer四command pages＋tests／必要concrete helper；`reads89`仍擁有全部shared與三read pages，禁止交叉寫入。primary10 command browser scenarios已建立（commands.mjs，syntax PASS，尚待遷移後執行），fixture issue retry保留原發票狀態以符合backend。
- Primary89 reads.mjs 7 scenarios與commands.mjs 10 scenarios已完成語法檢查，正式acceptance待對應pages穩定再跑；reads涵health401/503/raw degraded、analytics部分失敗、submitted notification filter、badge initial error與ERP payload retry/focus。兩 Terra/high writers持續各自owned slices，尚未宣稱89遷移完成。
- 首輪read browser：health401／503／raw degraded、analytics partial error、DLQ initial error badge PASS；notification submitted filter初跑因harness把「查詢」誤寫「搜尋」FAIL，locator已修正，但重跑在moving source於heading載入前timeout，未進驗證點（notification-filter.log／fixture-failure.json），不可視為PASS；Vite session10259證實command writer delete/recreate DLQ／Invoices／ERP期間缺module造成整App空白，primary已要求replacement準備好再atomic替換，暫停browser等routes可編譯。ERP drawer仍待command slice。reads89曾用package直接Vitest失去root jsdom config，primary已提示正確 `pnpm exec vitest run --project admin ...`，不可為document/localStorage環境錯誤改source。
- 89 Primary7read完整batch與10command完整batch皆PASS（reads-run.log／commands-run.log）。再加3command：ERP repeated unknown→drawer terminal rejection初次FAIL錯誤在modal背後，command writer修drawer local error後PASS；DLQ/contact held-refetch gate在修正harness徽章/狀態cell locator後PASS。新增identity-cancels-page-and-badge PASS。共8read／13command已分批PASS，runtime仍moving，最終須stable candidate checks/review。84layout matrix與root TC/test執行中。
- 89 readsslice已frozen，worker回報adminTC/tests/build、rootTC/561、diffPASS；primaryrootTC/561與84layout亦PASS，目視JA360Analytics／ERP可讀局部scroll。`review89`／`wA:p18` freshSol/high YOLO no-alt-screen已啟動，先審全runtime＋stable readtests；45runtime候選 `/tmp/storeweave89-runtime-review-candidate.json`。commands89最後test additions仍moving，完成後通知同一reviewer整票結論；尚非89done。
- commands89也已frozen：worker focused17／fullAdmin293、TC/build/diffPASS。全票76檔候選 `/tmp/storeweave89-source-candidate.json` 已存；primary finalAdminTC/build／26files293tests、8read／13command全批次重跑全部PASS（/tmp/storeweave89-admin-*.log、reads-final.log／commands-final.log），review89已收到allstable信號可合併整票。rootTC/561與84layoutPASS；目前等待freshSol結果，不宣告89done。
- Sol89首輪 **NEEDS WORK（test evidence only）**：`/tmp/storeweave89-review-round1.txt`，未發現P0/P1runtime缺陷。需補所有5command held-pending、ERP雙向row/drawer scope、invoice跨kind occupancy／wrong-kind；恢復DLQ lastError、invoice date/pending/void_pending、contactsubject/status既有斷言；補notification error、extension-error/degradedhealth isolation、later analytics refetch error。reads89只改3readpage tests，commands89只改4commandpage tests，runtime保持frozen。Badge ! protocol P3為nonblocking judgment，primary不擴model。補齊後同Sol增量，89仍in-progress。
- 89首輪proof fixes完成並frozen：reads89三頁23tests、commands89四頁33tests，adminTC/diffPASS，runtime45檔未變。candidate2 `/tmp/storeweave89-source-candidate2.json`76檔；review89已啟動僅七testfiles增量複核，primary finalAdmin round2 26files306testsPASS（/tmp/storeweave89-admin-test-round2.log），candidate2 hash仍一致。Badge ! P3 nonblocking無改動。
- Sol89第二輪仍為test-evidence-only NEEDS WORK：`/tmp/storeweave89-review-round2.txt`。只有invoice void需清除issue呼叫後獨立assert全部invalidation、void_failed fixture需有效issued欄位、contact需恢復未處理狀態斷言；其他首輪項目已接受。commands89僅擁有InvoicesPage.test.tsx／ContactInboxPage.test.tsx修正，runtime與其他tests frozen；之後同Sol最小增量複核。
- 第二輪兩檔test-only fixes完成：focused2suites18tests／adminTC／diffPASS。primary candidate3 `/tmp/storeweave89-source-candidate3.json`76hash確認只有Invoice／Contact tests變動，其他74檔與candidate2一致；review89已收最小增量複核，primary Admin round3重跑中。
- Primary89 browser harness `/tmp/storeweave89-primary-browser/{fixture.mjs,preflight.mjs}` 已建立，使用既有 Playwright/Chromium、不安裝依賴，API/health 皆攔截隔離；7 routes preflight全部PASS、零commands／pageErrors，results/screenshots同目錄。僅修改前fixture sanity，不代表Query migration驗收。localhost5175 Vite exec session10259仍供後續browser；88server已停止。
- Herdr `pane layout --current` 曾回傳 focused另一workspace w7:t3，但 `pane split --current` 實際遵循caller，返回新pane wA:p15／wA:t1。以 creation/get 回傳真實 ID 為準，勿操作無關w7或CMG pane。

### 2026-09-07 Ticket 90 完成驗收

- 最終Sol Standards／Spec PASS零發現；Admin314／root561／integration505／Docker62／native61、typechecks/build/diff、204layouts／36dialogs／12shell／3phase全PASS。完整證據與回復順序見Ticket90 Final acceptance；以下較早pending狀態保留為迭代歷程，已由本段取代。
- 90 owned closure90／review90 panes與Vite71350已完成清理；未提交或遠端寫入。

- 89已done，307admin／561root／browser8+13+84與Sol終審PASS；89三個owned panes已關閉、Vite10259已停止。
- authenticated gh issue27已核對；baseline715files `/var/folders/mp/2hbmdcp15qjfn3fhgctttgl40000gn/T/storeweave-ticket90-baseline-rnueccc1`。Herdr `analysis90`／`wA:p19` Sol/high YOLO no-alt-screen唯讀分析，先確定最小清理與完整驗收，再交Terra/high。
- Primary90browser準備 `/tmp/storeweave90-primary-browser`：複製88/89既有fixture與command/read/layout脚本至独立输出，88layout加Products形成9×12，89为7×12，另新shell-login.mjs涵12Login+12shellkeyboard。語法PASS但90尚未執行；需等清理後stable來源、啟動fresh5175再跑。未安裝依賴。
- Primary90 shell/Login preflight24checks（12shell鍵盤／palette回焦、12Login三語雙主題雙尺寸＋Tab順序）PASS，主代理目視JA360light無裁切。初跑腳本未等待Radix回焦造成false fail，改waitForFunction後全批PASS，無runtime修改。freshVite5175 exec71350供後續驗收；證據shell-login-preflight.log／shell-login-results.json。清理後須重驗。
- Sol90分析已完成接受 `/tmp/storeweave-ticket90-sol-analysis.txt`，analysis90pane已關閉。Herdr `closure90`／`wA:p1A` Terra/high YOLO no-alt-screen依 `/tmp/storeweave-ticket90-implementation-assignment.md`僅改7files（兩CSS、DESIGN、architecture、README、promotion-crud integration、smoke.sh）。Primary owns全integration／smokes／browser／tickets-spec-handoff；不可交叉寫入。
- Smoke隔離準備：`/tmp/storeweave90-compose.override.yaml`已config驗證api+worker image皆smoke90-20260907、uniqueproject storeweave-smoke90-20260907、port33190，native規劃33290。尚未啟動smoke。既有release2461檔hash `/tmp/storeweave90-preserved-release-hashes.json`及無關containers-before.txt已存，完後核對。
- closure90首輪7檔frozen：focusedpromotion10／root561／Admin307／兩typecheck／Adminbuild／diffPASS。Primary全integration **58files505testsPASS** `/tmp/storeweave90-integration.log`。Primary90browser108layout初跑在Products360發現summary四欄minwidth造成document636及pagination擠直排；84營運layoutPASS。已交closure90僅CSS修真正溢出，不許overflowhidden掩蓋，其他runtime保持frozen。
- Docker smoke第一次在Vitebuild中由primary有意取消exit130，讓最終artifact包含待修CSS；cleanup已跑、無matchingprojectcontainers。不是smokePASS或CI失敗。log `/tmp/storeweave90-smoke-docker.log`，修改後須重跑。Fullintegration通過不受CSS修改影響。source候選 `/tmp/storeweave90-source-candidate.json`715hash現需CSS完成後更新；freshVite71350仍alive。
- Products90CSS第一修正摘要卡已兩欄，primary round2發現pagination badge仍3行擠壓，已交closure90同CSS切片補nowrap／合理wrap，不能掩蓋。尚未重啟smoke。Primary實際Rollup bundle323module檢查PASS無backend/Node runtime，證據 `/tmp/storeweave90-admin-bundle-modules.json`／bundle-check.log（CSS再改只需正常build，modulegraph TS未變）。
- Native隔離目錄 pointer `/tmp/storeweave90-native-path`，實際 `/var/folders/mp/2hbmdcp15qjfn3fhgctttgl40000gn/T/storeweave90-native-xas22wa4` 已複製獨立node_modules與固定Node22.17.1x64cache（無install），尚未複製finalsource或執行smoke，既有release未動。
- ProductsCSS第二修正後primary108layoutPASS（layout108-round3.log）；84layout／shellLogin24清理後重跑PASS，primary目視JA360卻發現Products摘要／pagination寫死中文及currentpage狀態數未標範圍。已明確擴closure90唯一ownership至ProductsPage.tsx／ProductsPage.test.tsx／i18n.tsx，只改三語呈現與本頁／篩選total標示，禁止query/API/operation邏輯變動；review90已通知三檔moving，其他7檔可先審。
- FreshHerdr `review90`／`wA:p1B` Sol/high YOLO no-alt-screen唯讀獨立review，任務 `/tmp/storeweave-ticket90-review-assignment.md`；715hash candidate2 `/tmp/storeweave90-source-candidate2.json`需翻譯完成再更新。
- **Docker90round2 PASS 62/62** `/tmp/storeweave90-smoke-docker-round2.log`，exec22125完成exit0，uniqueproject containers已清；既有release2461hash及6無關dbcli容器均核對未變。此artifact早於待改Productcopy，backend／smoke契約未變，最終Adminbuild/browser及native必含finalcopy。native尚未run，先refresh其copy後再執行。
- Solreview90原7檔source Standards／Spec PASS、零發現；接受Docker62/62對未變backend/script契約有效，整票仍待Products/i18n/test增量与finalAdmin/browser/native。closure90補譯包含Products庫存drawer既有提示，保留rawpreset理由payload，不改操作邏輯；尚在working，不因waittimeout重啟。
- Products三語增量frozen：focused44／Admin26files310tests／adminTC/build/diffPASS，rawstockreasonpayload保持中文原值，只翻譯labels。candidate3 `/tmp/storeweave90-source-candidate3.json`715hash；review90已啟動3file增量。Primary108final＋36productdialogs exec47023跑中；nativeisolated已refresh來源、exec2515跑中（/tmp/storeweave90-smoke-native.log），tarball已建好、正啟動Debian。不可將執行中當PASS。
- Nativefirstattempt及diagnostic重現product201但invalidJSON/無id，已定位**驗證shell環境**：native暫存cwd解析`/bin/bash`3.2，repo解析HomebrewBash；同一37line repro僅切Bash即FAIL↔PASS，直接HTTP JSON亦PASS。無API缺陷證據，不改backend。診斷 `/tmp/storeweave90-native-diagnosis.md`，diagnosticownednative容器/network已清；下次native需明確HomebrewBash與childPATH、恢復cleanup EXIT，等copyfinal再跑。
- Sol90增量3findings `/tmp/storeweave90-review-round1.txt`：productpending誤用付款pending／unknownError文案、stockforecast件硬編碼/筆單位誤用、英文全形冒號；已交closure90同3檔修正並加非零stockforecastlocale測試，primary36dialogs需加quantitygate。108+36上一候選PASS不能覆蓋此forecast分支。
- Round1翻譯finding修正後candidate4 `/tmp/storeweave90-source-candidate4.json`715hashes，runtime/stable：focused47／Admin26files313tests／TC/build/diffPASS；primary36dialogs含qty5forecast全PASS，目視JA360stockforecast可讀units正確。Review90三檔增量跑中。NativeBash5round2 exec96248（/tmp/storeweave90-smoke-native-round2.log）freshbuild63MBtarball、Debian安裝中；isolatedscript恢復cleanupEXIT且forcefreshbuild避免無git快照重用舊artifact，rootnative腳本未改。
- 最終nativeBash5round2 **61/61PASS** exit0，/tmp/storeweave90-smoke-native-round2.log；ownedsmoke容器/network已清，既有release2461檔及6無關容器核對未變（preservation-result.json）。Native少1check為hostDEMO_ERP_API_KEY條件，Docker62已覆蓋。
- Solround2追加3finding `/tmp/storeweave90-review-round2.txt`（pending/unknown需distinct文案、preview標點localeassert、unusedexample/previewkeys），closure90已修；candidate5 `/tmp/storeweave90-source-candidate5.json`715hash，只有Product/i18n/test與primaryhandoff改動。Admin314/build/diffPASS，primary3語phasebrowserPASS，36dialogs最後重跑含forecast+pricepreview進行中；review90最小增量審查中。Backend/root/integration/native/Docker保持accepted，最後僅copy+tests變動。
- 計畫與read-only smoke preparation見Ticket90及`/tmp/storeweave-ticket90-preflight.md`。現有release與無關dbcli容器不可移除；integration促銷共用fixture與smoke事件數baseline問題需最小修復。90完成後仍接BaseB00–B17，不縮減總目標。

## 第一個接手入口

本輪實作已授權持續進行，不是「只做一票」：依賴順序完成 Ticket 82–90，隨後依 [Base 計畫](base-implementation-plan.md) 的 DAG 完成 B00–B17；每票實作、驗證、指定獨立審查與本文件更新後，自動進入下一張可執行票。現在的唯一實作入口為 [Base B02](base/b00/next-work-cards.md)。

Ticket 84 已完成：DateField 改為 Radix Popover、Brand Content RowMenu → Delete Dialog → Escape 回焦已測、四張固定表格窄版改為水平捲動；admin（21 suites／194 tests）、根層（48 files／561 tests）、build、三語 × 明暗 × 360／1280 隔離 browser matrix 與 Terra/high 獨立複核均通過。詳見 [Ticket 84](tickets/84-admin-marketing-content-ui.md)。
Ticket 85 已完成：四個 commerce operations pages、共用 reason/menu primitive 接合、三語、reentry guards、退款與會員調帳的 idempotency payload snapshot／鎖定與失敗保留已實作。`pnpm typecheck:admin`、admin 21 files／200 tests、build、root 48 files／561 tests、`git diff --check` 皆通過；隔離 browser 已覆蓋三語／雙主題、RMA Escape 回焦與 360px Shipping。Sol/high 第一輪與終審先後提出 timeout payload/key 不一致和 ReasonDialog no-op error-dismiss，均修正並加 regression；第二次全新 Sol/high 終審 PASS，review pane 已關閉。
Ticket 81 已完成：Order browser projection 由 DTO 的 type-only 明確投影導出，adjustment source 不再手寫；Sol/high 複核與隔離 browser smoke 通過。Ticket 82 已完成：Dialog／Dropdown Menu shadcn source-distribution slices 以 Radix 1.1.23／2.1.24 接入，RowMenu／ReasonDialog 已遷移；所有驗證、三語×雙主題×兩斷點 browser matrix 與 Terra/high 獨立複核均通過。詳見對應本地 Ticket 的 implementation record。
Ticket 83 已完成：Products create/edit/stock 遷移到 Dialog primitive，保留 native table 與批次庫存查詢，新增列選單→庫存 Dialog→Escape 回到 trigger 的 regression；`pnpm typecheck:admin`、`pnpm test:admin`（21 suites／189 tests）、`pnpm build:admin`、`pnpm typecheck`、`pnpm test`（48 files／561 tests）與隔離 browser matrix 都通過。Terra/high 首輪發現 RowMenu 回焦目標與殘留 `h3` CSS，修正後 PASS；review pane 已關閉。
這些是派工設定，不表示本交接文件已更改 Codex 的模型或 reasoning 設定。

1. 讀適用規則、`git status`、HEAD；保留完整未提交的81–90成果，不重做已驗收票。
2. 讀 [Spec0009](specs/0009-complete-modular-base.md)、[Base計畫](base-implementation-plan.md)、[B00 完成證據](base/b00/README.md) 、[B01 完成證據](base/b01/README.md) 與 [B02 派工卡](base/b00/next-work-cards.md)；依既定DAG完成B02–B17，不縮為MVP。
3. Base工作包沒有對應GitHub issue。B00已完成盤點／14 probes／ADR草案與雙軸Sol複審，保留現有Queue並在B04補可靠性；不得重做B00或把PoC誤標為完整Base。
4. 連續執行已授權。高風險核心由主代理持有，Sol/high分析與獨立審查；有界委派依使用者Terra/high與Herdr YOLO設定，單檔單writer。
5. 本文件保留後台歷程；Base進度更新Base計畫與相應工作包證據。每包完整驗證、獨立審查後才接下一個可執行前沿。未commit／push／merge／GitHub寫入／部署。

## 可直接交給接手模型的指令

> 接手StoreWeave Base B02。先讀本交接、Spec0009與Base執行計畫；81–90與B00–B01已完成，不重做。
> 使用Herdr、指定模型與YOLO依DAG持續完成B00–B17，保留完整目標與dirty worktree。
> 高風險核心留主代理、Sol/high分析與獨立審查；不commit、push、merge、部署或寫GitHub。
