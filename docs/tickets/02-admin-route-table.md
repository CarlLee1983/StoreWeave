# 02 — Admin 路由表重構

**What to build:** 後台的頁面與導覽由一份路由表資料結構驅動，而不是散在三處的寫死字串聯集、導覽陣列與標題對照表。新增一個頁面從此只是加一列資料。行為對使用者完全不變。

**Blocked by:** None — can start immediately

**Status:** done

- [x] 路由、導覽項目、頁面標題由單一份資料結構產生
- [x] 既有五個頁面的導覽、切換、標題與命令面板行為不變，並有測試佐證
- [x] 路由仍是編譯期已知的 —— 不引入執行期註冊機制
