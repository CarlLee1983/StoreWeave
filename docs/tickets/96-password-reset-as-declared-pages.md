# 96 — 忘記密碼與重設密碼走模組宣告的頁面

**What to build:** 忘記密碼的人在前台送出信箱、收到連結、設定新密碼、被帶到登入頁——四條路由從 controller 搬到模組宣告的頁面，行為一字不改。

**Blocked by:** 94

**Status:** ready-for-agent

- [ ] 忘記密碼與重設密碼的 GET／POST 由模組宣告，controller 上對應的 decorator 刪除
- [ ] 忘記密碼不論信箱存不存在都回同一句中性訊息，失敗只記 log
- [ ] 重設成功後轉到登入頁；token 無效時的訊息與狀態碼不變
- [ ] 兩者的節流行為不變——忘記密碼沒有節流就是免費的寄信轟炸器

## 邊界

和 95 改的是不同頁面，可以並行；共用的模組骨架在 94 就位了。
