# 68 — 後台退貨案件工作台

**What to build:** 工單 65 的 RMA 審核流程只存在於 API：`apps/admin/src/api.ts` 的七支 client
方法與 `/api/v1/rmas` 端點都在，但後台沒有頁面，店員點不到。補上一張路由表驅動的頁面，
把「列出待處理案件 → 檢視行項 → 審核／補件／拒絕／收件／請退款」走完。

**Blocked by:** —

**Status:** completed

- [x] 路由表加一列 `rmas`，側欄、標題與命令面板跟著出現，不另外維護第二份清單
- [x] 可依狀態與訂單 ID 篩選案件；預設看得到 `requested` 這批待處理的
- [x] 案件詳情顯示行項、數量、折後淨額、申請原因、店員備註與退款紀錄連結
- [x] 動作按鈕依目前狀態決定可用性，不讓店員送出狀態機不允許的轉換
- [x] 補件與拒絕強制填備註；收件要為每一行選 restock／discard，discard 必須寫原因
- [x] 以 jsdom 元件測試覆蓋篩選、狀態閘門與必填備註

## 不做的事

- 部分退款的金額編輯（工單 65 已記為刻意不做）、換貨出貨、RMA 的顧客通知樣板。
