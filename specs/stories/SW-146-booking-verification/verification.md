# SW-146 Booking repository verification record

## Scope and revision

This record maps all 17 conditions in [Spec 0011 §9](../../../docs/specs/0011-booking-product-release.md) to repository evidence. The application source revision selected for verification is `d1e707e8342e7a1336d829dd17fe36c1cfcf142d`. The initial evidence record was delivered in documentation commit `16e435b1d9a77296df00be6c9019aa9a35fe5a3b`; its parent is the application source revision. The full gate below ran in the PR worktree with that documentation-only commit. Earlier Story results do not establish the gate result for this revision.

The required command is `make verify` from the repository root. [Makefile](../../../Makefile) runs, in order, `pnpm typecheck`, `pnpm typecheck:admin`, `pnpm test`, `pnpm test:admin`, and `pnpm test:integration`. Unit/architecture tests are in `pnpm test`; Admin tests are in `pnpm test:admin`; PostgreSQL integration tests are in `pnpm test:integration` and require Docker/testcontainers. All five targets must exit 0 at the recorded source revision. Focused checks can diagnose a failure but cannot replace this gate.

**Full repository gate:** `make verify > /tmp/storeweave-sw146-make-verify.log 2>&1` exited **0** on `2026-09-25 01:35 UTC` in the PR worktree at evidence commit `16e435b1d9a77296df00be6c9019aa9a35fe5a3b`, whose application source parent is `d1e707e8342e7a1336d829dd17fe36c1cfcf142d`. Both typechecks passed; unit: 155 files / 1,524 tests; Admin: 32 files / 351 tests; integration: 115 files / 1,021 tests. Docker 29.4.0 was available. The primary worker confirmed the exit status and clean PR worktree after the gate; the local log's SHA-256 was independently checked as `1c732435fe22361ff706b2338d37a92bc20d65bc91b9f062596dc36b71c72641`. Artifact: `/tmp/storeweave-sw146-make-verify.log` (local temporary file, **not durable in the PR**). The clean PR diff from the application source contained only the three SW-146 documentation files. This pass covers the repository gate; it does not fill the specific evidence gaps or clear external release gates below.

**Durable CI artifact:** [PR #108 Actions run 36081990094](https://github.com/CarlLee1983/StoreWeave/actions/runs/36081990094) concluded **success** at `2026-09-25T01:35:52Z` for head `16e435b1d9a77296df00be6c9019aa9a35fe5a3b`. All 10 checks succeeded, including typecheck/unit, full integration, both integration shards, and smoke. This CI run is on the earlier documentation commit, with the same application source revision; the later documentation-only record corrections are not in that CI head. It complements the local full-gate log and does not clear the §9 coverage gaps or external gates.

## §9 evidence matrix

Paths below are test artifacts in this repository and were included in the passing full gate, subject to the stated coverage limits. `U` means `pnpm test`, `A` means `pnpm test:admin`, `I` means `pnpm test:integration`, and `TC` means the two typecheck targets. Every route is included in `make verify` unless a separate command is named. A passing command does not turn an unasserted subcondition into evidence.

| §9 | Executable evidence / artifact location | Coverage at source revision and limit |
| --- | --- | --- |
| 1 | U: `packages/releases/{base,commerce,booking}/test/*release.test.ts`, `packages/platform/release/test/module-contract.test.ts`, `packages/themes/{base,default,booking-default}/test/*theme.test.ts`, `packages/releases/commerce/test/extension-contracts.test.ts`, `packages/platform/extension-sdk/test/payment-contract.test.ts`; I: `tests/integration/booking-public-http.test.ts`; U: `tests/unit/booking-callback-http.test.ts`, `tests/architecture/booking-admin-projection.test.ts`, `tests/architecture/booking-*-admin-artifact.test.ts`. TC checks backend and Admin types. | Executed in passing gate. These cover the named release/contract surfaces in aggregate; no single test asserts every Release × module/Theme/Extension/HTTP/Admin combination. |
| 2 | I: `tests/integration/booking-clean-start.test.ts` migrates a fresh PostgreSQL database and checks its catalog and selected artifacts. U: `tests/architecture/release-isolation.test.ts`, `tests/architecture/booking-admin-projection.test.ts` inspect target graphs. | Executed in passing gate; clean database and emitted browser/server graph assertions are local evidence, not deployed artifact inspection. |
| 3 | U: `tests/architecture/release-baseline.test.ts`; I: `tests/integration/migration-history.test.ts`, `tests/integration/release-artifacts.test.ts`, `tests/integration/release-transition.test.ts`, and Commerce integration regressions in the full gate. | Executed in passing gate; earlier SW-105/Commerce results alone could not certify this revision. |
| 4 | U: `packages/platform/extension-sdk/test/payment-contract.test.ts`, `packages/extensions/{mock-payment,ecpay}/test/provider.test.ts`; I: `tests/integration/cart-checkout.test.ts`, `tests/integration/refund-domain.test.ts`, `tests/integration/booking-reservation.test.ts`. | Local contracts executed in passing gate. ECPay merchant staging refund UAT is a separate unresolved gate; scripted provider fixtures do not clear it. |
| 5 | I: `tests/integration/booking-availability-quote-reservation.test.ts` (`serializes two attempts for the final unit`), `packages/booking/availability/test/room-night-operations.integration.test.ts`, `tests/integration/booking-e2e.test.ts` (`one winner for the last Room Nights`). | Executed in passing gate; PostgreSQL race cases exercise the final unit. |
| 6 | I: `tests/integration/booking-availability-quote-reservation.test.ts` (`returns refreshed terms for changed price or policy` and final-unit case), `tests/integration/booking-e2e.test.ts` (`no rows for stale or unavailable Quotes`); U: `packages/booking/availability/test/quote-reservation.test.ts`. | Executed in passing gate; tests distinguish refreshed terms from unavailable supply. |
| 7 | I: `tests/integration/booking-reservation.test.ts` (one-shot expiry, cancellation, asynchronous refund failure), `tests/integration/booking-e2e.test.ts` (expiry frees both nights; cancel/refund journey). U: `packages/booking/reservation/test/cancellation-policy.test.ts`. | Executed in passing gate; refund failure leaves cancellation and Room Night release intact in the covered case. |
| 8 | I: `tests/integration/booking-reservation.test.ts` (deadline extension versus queued expiry, callback replay, concurrent cancellation, lock-wait assertions), `tests/integration/booking-e2e.test.ts` (two callbacks, one winner). | Covered cases executed in passing gate. **Missing direct automated evidence, issue #109:** callback versus cancellation and callback versus expiry racing under the PostgreSQL lock with asserted serializable outcomes. Existing cases include extension versus expiry and sequential callback after cancellation/expiry; these do not prove the full four-actor concurrency wording. |
| 9 | I: `tests/integration/booking-reservation.test.ts` (fresh Attempt after definite failure/expiry, parallel active Attempt rejection, callback replay and Excess refund), `tests/integration/booking-e2e.test.ts` (two successful callbacks, one winner/one refunded Excess). | Executed in passing gate. |
| 10 | I: `tests/integration/booking-reservation.test.ts` (late callback, initiation/callback race, Attempt-linked refund/alert), `tests/integration/booking-e2e.test.ts` (cancelled deferred Reservation stays released after signed late callback), `tests/integration/booking-release-journey.test.ts` (operator alert). | Local behavior executed in passing gate. Real SMTP delivery remains a separate gate. |
| 11 | I: `tests/integration/booking-reservation.test.ts` (single-use grant, hash-only management credential, invalid/expired/replayed grants, revocation), `tests/integration/booking-public-http.test.ts` (`redacts credentials and unrelated PII from every management response and captured diagnostic`, including the grant and management credential), `tests/integration/booking-e2e.test.ts` (clean redirect and denied unauthenticated access). U: `packages/booking/reservation/test/{notifications,checkout-access}.test.ts`. | Executed in passing gate. Captured HTTP diagnostics directly assert token exclusion; this is not an instrumented inspection of every possible production log sink. |
| 12 | I: `tests/integration/booking-reservation.test.ts` (management proof plus Account identity; competing claims; revoked access), `tests/integration/booking-e2e.test.ts` (explicit claim). U: `tests/architecture/booking-reservation-boundaries.test.ts`. | Executed in passing gate; client account id/email alone are not accepted as ownership evidence in these paths. |
| 13 | I: `tests/integration/booking-availability-quote-reservation.test.ts`, `tests/integration/booking-availability.test.ts`, `tests/integration/booking-e2e.test.ts`; U: `packages/booking/availability/test/{search,types,quote,quote-reservation}.test.ts`. `quote.test.ts` rejects two rooms with one adult and excess guests, accepts a two-room Quote, and rejects insufficient units. | Covered cases executed in passing gate. **Missing direct automated evidence, issue #110:** a real PostgreSQL multi-room Reservation that proves occupancy and inventory constraints together under contention. Separate unit constraints and one-room integration cases do not establish that result. |
| 14 | U: `packages/releases/booking/test/booking-release.test.ts` (missing Storefront renderer fails naming its key), `tests/architecture/booking-admin-projection.test.ts` (missing/duplicate/stale Admin keys and missing UI entries), `tests/architecture/booking-*-admin-artifact.test.ts`; I: `tests/integration/booking-release-journey.test.ts` (selected renderer journey). | Executed in passing gate. Direct missing-contribution failures exist. Booking projection is typechecked by `pnpm typecheck:admin`, but the shared Admin shell is typechecked with Commerce selection; Booking browser graph checks are complementary, not a Booking-selected shell typecheck. |
| 15 | U: `tests/architecture/release-isolation.test.ts`, `tests/architecture/release-isolation-negative.test.ts`, `tests/architecture/booking-release-boundaries.test.ts`, `tests/architecture/booking-admin-projection.test.ts`. Real target graphs and synthetic violations cover forbidden imports and Commerce leakage. | Executed in passing gate; test assertions and log are the scan evidence at this revision. |
| 16 | I: `tests/integration/booking-reservation.test.ts` (PII redaction, retained winner/refund/invocation/audit evidence, multiple batches), `tests/integration/booking-e2e.test.ts` (before/after retention snapshots). U: `packages/booking/reservation/test/retention-date.test.ts`. | Executed in passing gate. |
| 17 | Root `Makefile` and this record's `make verify` command/result block. | **Passed:** exit 0 at the recorded application source and documentation commit. ECPay staging UAT and deployment are independent gates. |

## Separate external release gates

These require their own environment, operator, result, revision/configuration, and retained artifact. A local mock, test transport, earlier Story result, or `make verify` pass cannot clear them.

| Gate | Current evidence and status | Evidence needed to clear |
| --- | --- | --- |
| ECPay staging refund UAT | **Unresolved.** `specs/stories/SW-116-ecpay-refund-contract/staging-uat-evidence.md` states merchant UAT is uncleared; local adapter tests use scripted responses. | Merchant-enabled staging refund execution and reconciliation with provider references, redacted request/response/result, environment/config, and operator sign-off. |
| Real SMTP | **Unresolved.** Booking tests use local notification fixtures/transports. | Delivery and failure/retry evidence through the intended SMTP service, with recipient/config, redacted message evidence, and operator sign-off. |
| Production config and deployment | **Unresolved.** Clean-start and artifact tests use disposable/local configuration. | Reviewed Booking production configuration and secrets, deployment/migration result, health/smoke evidence, revision/artifact identity, and rollback readiness. |
| Operator acceptance | **Unresolved.** Automated assertions do not record business/operator approval. | Dated acceptance against the deployed Booking journey and operational refund/notification procedures, with named approver and artifact. |

## Missing repository evidence before completion

The matrix is an inventory of runnable paths, not an assertion that §9 is fully proved. §9.8 still needs direct callback/cancellation and callback/expiry concurrent lock-order assertions (blocker #109). §9.13 still needs a PostgreSQL multi-room Reservation case combining occupancy and available-unit enforcement under contention (blocker #110). §9.1 has only aggregate contract coverage rather than an explicit three-Release-by-surface inventory. §9.11 covers captured HTTP diagnostics but not every possible production log sink. §9.14 has direct missing renderer/Admin key failures and emitted Booking browser checks; `pnpm typecheck:admin` does not select Booking for the shared Admin shell. PR #108 remains blocked from completion on #109 and #110 even though the full repository gate passed. Do not close `AC-001` or SW-146 on the basis of the current mapping alone. Adding the missing tests is outside this documentation-only slice.

## Record preparation checks

At `2026-09-25 01:24:00 UTC`, during preparation of the initial evidence commit, the SW-146 documentation worktree had application source HEAD `d1e707e8342e7a1336d829dd17fe36c1cfcf142d`:

| Command / review | Result | Artifact |
| --- | --- | --- |
| `python3 -m json.tool specs/stories/SW-146-booking-verification/readiness.json` | Exit 0; JSON parsed. | `readiness.json` |
| Python `hashlib.sha256` comparison of `story.md` and `acceptance.md` bytes to both `readiness.json` digests; ordered §9 row check 1–17; key test/link existence assertions | Passed; 17 ordered rows, both digests, links, and named evidence paths matched. | `verification.md`, `acceptance.md`, `readiness.json` and cited repository paths |
| `git diff --check` plus whitespace review of `verification.md` (then a new file) | No whitespace errors. | Documentation-only worktree diff |

These are record/contract checks only. The later full-gate result above clears `AC-002`; these focused checks did not do so and cannot fill the missing automated evidence.

## Failure and missing-evidence rule

Do not mark SW-146 complete when `make verify` fails, is interrupted, cannot start Docker/testcontainers, has no preserved exit status/artifact, or ran on a stale revision. Record the command, revision, timestamp, failing target/test or missing environment, log location, and subsequent focused rerun separately. Preserve the failure; a focused green rerun does not erase a failed full gate. Re-run the complete gate after a source change. Verification must stay read-only; do not alter product source or weaken checks to manufacture a pass. Mock ECPay or local SMTP outcomes cannot be substituted for the external gates.

Prior failure evidence: `specs/stories/SW-144-release-isolation/acceptance.md` records two earlier complete `make verify` attempts that each reached integration and failed one timing-sensitive case (`full-restore.test.ts` database session at cutover; `worker-execution.test.ts` 500 ms heartbeat timeout). Focused reruns passed, and a third complete gate passed for that earlier worktree. These are examples of retaining failed-run evidence, not results for this SW-146 revision. The SW-146 full gate has the separate passing result and artifact recorded above.
