# Spec 0011 §9.1 contract surfaces

This inventory separates checks that run on a selected release from checks of the shared contract checker or an individual implementation. Passing the latter does not prove every release and surface combination.

| Release | Module | Theme | Extension | HTTP | Admin |
| --- | --- | --- | --- | --- | --- |
| Base | `packages/releases/base/test/base-release.test.ts` checks selected composition; no selected Base module calls `runModuleContractChecks` here. | `packages/themes/base/test/base-theme.test.ts` checks Base renderers. | Base selects no Extension (`packages/releases/base/test/base-release.test.ts`). | `tests/integration/base-release.test.ts` exercises the selected Base server. | Base's disabled projection is checked in `packages/releases/base/test/base-release.test.ts`. |
| Commerce | `packages/releases/commerce/test/commerce-release.test.ts` checks selected composition and frozen public contracts; no selected Commerce module calls `runModuleContractChecks` here. | `packages/themes/default/test/default-theme.test.ts` checks Default renderers. | `packages/releases/commerce/test/extension-contracts.test.ts` calls `runExtensionContractChecks` for available Extensions. | `tests/architecture/b17-public-contract-semantic.test.ts` checks the frozen Commerce HTTP surface. | `tests/architecture/admin-projection-preflight.test.ts` and `admin-projection-artifact.test.ts` check composition and browser artifact. |
| Booking | `tests/architecture/booking-module-contracts.test.ts` calls `runModuleContractChecks` on each selected Booking product module; Property and Availability pass all checks, while Reservation has only the two pinned internal-only diagnostics below. | `packages/themes/booking-default/test/booking-theme.test.ts` checks renderers and a missing-renderer failure. | `tests/architecture/booking-module-contracts.test.ts` invokes `runExtensionContractChecks` on the selected `mock-payment` definition; `packages/platform/extension-sdk/test/payment-contract.test.ts` also checks the provider contract. | `tests/integration/booking-public-http.test.ts` exercises selected HTTP routes and excludes internal command targets/permissions from the HTTP catalog; `tests/unit/booking-callback-http.test.ts` checks callback mapping. | `tests/architecture/booking-admin-projection.test.ts` checks selected contributions, permissions, failure cases and browser graph. |

The shared checker itself has positive and negative tests in `packages/platform/release/test/module-contract.test.ts`. The file-requests example invokes it on its selected module in `packages/examples/file-requests/test/contract.test.ts`. Those tests do not invoke it on Base, Commerce or Booking product selections.

## Current Booking module result

`node_modules/.bin/vitest run --project unit tests/architecture/booking-module-contracts.test.ts` passed 7/7. `node_modules/.bin/vitest run --project integration tests/integration/booking-public-http.test.ts` passed 19/19. `node_modules/.bin/tsc -p tsconfig.json --noEmit` passed. The Reservation helper emits exactly two pinned diagnostics:

1. `booking-reservation:system-write` and `booking-reservation:retention-write` have no explicit named grant in selected human roles. The helper requires every declared permission to appear by name in a release role. `SYSTEM_ACTOR` holds both via `*`; the selected Admin role also has a wildcard operator grant, so this check does not prove Admin denial. Other selected human roles do not hold them. The Booking HTTP catalog exposes neither permission nor any command that requires either permission.
2. `booking.reservation.materializeNotification` is an internal discriminated input schema. The helper's generic HTTP plain-object inspection cannot traverse it; a test verifies that all four real variants accept valid input and reject an extra key. The Booking HTTP catalog excludes the command.

The architecture test asserts the exact raw diagnostics above. Any new failure, missing expected diagnostic, or scheduled-job contract failure turns it red. The refund reconciliation payload mismatch found by the initial run was corrected in the owning Booking Reservation module under SW-159; the interval schedule contract now passes. The selected mock-payment Extension has no subscribed events or requested permissions, so the test checks those lists are empty before supplying empty platform catalogs to the SDK checker. The current focused checks do not replace the required full `make verify` gate.

The integrated full gate subsequently passed at [follow-up checkpoint 4](follow-up-verification.md#integrated-checkpoint-4), with unchanged source digest. The focused results above remain the narrow evidence for these contract assertions.
